#ifndef OZTAS_T3_TASKS_HPP
#define OZTAS_T3_TASKS_HPP

#include "matrix.hpp"

namespace oztas
{
  int countNonZeroDiagonals(const int* matrix, size_t rows, size_t cols);

  void applyFillIncreasingWave(int* matrix, size_t rows, size_t cols);
}

#endif
