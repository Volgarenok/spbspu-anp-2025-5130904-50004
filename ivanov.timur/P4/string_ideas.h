#ifndef STRING_IDEAS_H
#define STRING_IDEAS_H

namespace ivanov {
  void spc_rmv(char* content, size_t size);
  void rmv(char* conent, size_t size, size_t id);
  void del_lat(char* content, size_t size);
  void output(char* content);
  char* merge(char* content1, char* content2);
}

#endif
