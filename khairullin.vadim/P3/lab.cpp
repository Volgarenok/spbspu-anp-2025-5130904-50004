#include <iostream>
#include <fstream>

int main (int argc, char ** argv)
{
  if (argv[1] == 1)
  {
    int array[10000] = {};
  }
  else if (argv[1] == 2)
  {
    int * array = reinterpret_cast<int*>(rows * cols * sizeof(int));
  }
}
